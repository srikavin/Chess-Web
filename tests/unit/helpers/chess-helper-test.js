import {chessHelper} from 'chess/helpers/chess-helper';
import {module, test} from 'qunit';

module('Unit | Helper | chess helper');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = chessHelper([42]);
  assert.ok(result);
});

